import { combineReducers } from "redux";


import course from './course';

export default combineReducers({
    course
});

//com o combine os objetos passam a ser chamados como são setados acima
// couse: { modules:[]...}